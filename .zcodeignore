# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# 运行时产物（照片输出目录）
photos/

# 模型权重（体积大，由 scripts/download_models.py 拉取）
models/
*.onnx
*.pth
*.tflite

# 大文件：141MB 的答辩 PPT 超过 GitHub 100MB 单文件限制，不入库
*.pptx

# IDE
.idea/
.vscode/

# MediaPipe Tasks 模型
*.task

# headless 批跑输出
outputs/

# ===== ↑ 以上同步自 .gitignore（「从 .gitignore 同步」只重写以上部分）=====
.git/
.hg/
.svn/
node_modules/
bower_components/
jspm_packages/
site-packages/
coverage/
htmlcov/
lcov-report/
cmakefiles/
cmake-build-*/
bazel-*/
pods/
deriveddata/
storybook-static/
playwright-report/
test-results/
allure-results/
allure-report/
cdk.out/
*.dist-info/
eggs/
pip-wheel-metadata/
wheels/
# ----- ↑ 以上为 ZCode 默认排除规则（自定义规则请写在本行下方，不会被同步/恢复改动）-----
# 自定义规则写在下方（本行提示可删除）
